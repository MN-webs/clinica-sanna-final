{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Preparando entorno web Java...'"
  },
  "deploy": {
    "startCommand": "java -jar target/dependency/webapp-runner.jar ROOT.war || java -cp 'WEB-INF/classes:WEB-INF/lib/*' jakarta.servlet.http.HttpServlet || jar -xvf ROOT.war && java -jar target/app.jar",
    "restartPolicyType": "ON_FAILURE"
  }
}
