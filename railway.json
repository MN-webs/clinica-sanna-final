{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p target/app && cd target/app && jar -xvf ../../ROOT.war && java -cp 'WEB-INF/classes:WEB-INF/lib/*' jakarta.servlet.http.HttpServlet || java -jar ../../ROOT.war",
    "restartPolicyType": "ON_FAILURE"
  }
}
 
